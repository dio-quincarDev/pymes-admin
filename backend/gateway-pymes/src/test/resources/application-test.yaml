# Gateway Test Configuration
# Se definen valores por defecto para que el contexto de Spring no falle al levantar
AUTH_SERVICE_HOST: ${AUTH_SERVICE_HOST_TEST:localhost}
REDIS_HOST: ${REDIS_HOST_TEST:localhost}
REDIS_PORT: ${REDIS_PORT_TEST:6379}

jwt:
  # Secreto mock para tests unitarios del Gateway
  secret: ${JWT_SECRET_TEST:988cf74e0e513f5f56af0f421f83c2169e0f49a0a780096469dfc4d4102cfab3_TEST_ONLY}
