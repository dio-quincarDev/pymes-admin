name: CD - Deploy to Staging
run-name: Deploy staging de ${{ github.event.workflow_run.head_branch }} por @${{ github.event.workflow_run.actor.login }}

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.event.workflow_run.head_branch }}
  cancel-in-progress: true

on:
  workflow_run:
    workflows: ["CI - Build and Test"]
    types: [completed]
    branches: [develop]

env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}

jobs:
  # ==================== VERSION ====================
  build-and-test:
    name: Generate Version
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_repository.full_name == github.repository && github.event.workflow_run.event == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Generate version
        id: version
        run: echo "version=$(date +%Y%m%d)-${GITHUB_SHA:0:7}" >> $GITHUB_OUTPUT

  # ==================== DOCKER BUILD AMD64 ====================
  docker-build-amd64:
    name: Build Docker Images (amd64)
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_repository.full_name == github.repository && github.event.workflow_run.event == 'push' }}
    runs-on: ubuntu-latest
    needs: [build-and-test]
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          persist-credentials: false

      - name: Login to Docker Hub
        uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@6524bf65af31da8d45b59e8c27de4bd072b392f5 # v3.8.0

      - name: Build and push Auth (amd64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./backend/auth
          platforms: linux/amd64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-auth:staging-amd64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-auth:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-auth:buildcache-v2,mode=max

      - name: Build and push Core (amd64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./backend/core
          platforms: linux/amd64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-core:staging-amd64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-core:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-core:buildcache-v2,mode=max

      - name: Build and push Gateway (amd64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./backend/gateway-pymes
          platforms: linux/amd64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-gateway:staging-amd64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-gateway:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-gateway:buildcache-v2,mode=max

      - name: Build and push Frontend (amd64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./frontend/pymes
          platforms: linux/amd64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-frontend:staging-amd64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-frontend:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-frontend:buildcache-v2,mode=max

  # ==================== DOCKER BUILD ARM64 ====================
  docker-build-arm64:
    name: Build Docker Images (arm64)
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_repository.full_name == github.repository && github.event.workflow_run.event == 'push' }}
    runs-on: ubuntu-latest
    needs: [build-and-test]
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          persist-credentials: false

      - name: Login to Docker Hub
        uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@6524bf65af31da8d45b59e8c27de4bd072b392f5 # v3.8.0

      - name: Build and push Auth (arm64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./backend/auth
          platforms: linux/arm64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-auth:staging-arm64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-auth:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-auth:buildcache-v2,mode=max

      - name: Build and push Core (arm64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./backend/core
          platforms: linux/arm64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-core:staging-arm64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-core:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-core:buildcache-v2,mode=max

      - name: Build and push Gateway (arm64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./backend/gateway-pymes
          platforms: linux/arm64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-gateway:staging-arm64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-gateway:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-gateway:buildcache-v2,mode=max

      - name: Build and push Frontend (arm64)
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5.4.0
        with:
          context: ./frontend/pymes
          platforms: linux/arm64
          push: true
          tags: ${{ env.DOCKER_USERNAME }}/pymes-frontend:staging-arm64
          cache-from: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-frontend:buildcache-v2
          cache-to: type=registry,ref=${{ env.DOCKER_USERNAME }}/pymes-frontend:buildcache-v2,mode=max

  # ==================== DOCKER MANIFEST ====================
  docker-manifest:
    name: Create Manifest Lists
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_repository.full_name == github.repository && github.event.workflow_run.event == 'push' }}
    runs-on: ubuntu-latest
    needs: [build-and-test, docker-build-amd64, docker-build-arm64]
    permissions:
      contents: read

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
        with:
          username: ${{ env.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@6524bf65af31da8d45b59e8c27de4bd072b392f5 # v3.8.0

      - name: Create Auth manifest
        run: |
          docker buildx imagetools create -t ${{ env.DOCKER_USERNAME }}/pymes-auth:staging \
            ${{ env.DOCKER_USERNAME }}/pymes-auth:staging-amd64 \
            ${{ env.DOCKER_USERNAME }}/pymes-auth:staging-arm64

      - name: Create Core manifest
        run: |
          docker buildx imagetools create -t ${{ env.DOCKER_USERNAME }}/pymes-core:staging \
            ${{ env.DOCKER_USERNAME }}/pymes-core:staging-amd64 \
            ${{ env.DOCKER_USERNAME }}/pymes-core:staging-arm64

      - name: Create Gateway manifest
        run: |
          docker buildx imagetools create -t ${{ env.DOCKER_USERNAME }}/pymes-gateway:staging \
            ${{ env.DOCKER_USERNAME }}/pymes-gateway:staging-amd64 \
            ${{ env.DOCKER_USERNAME }}/pymes-gateway:staging-arm64

      - name: Create Frontend manifest
        run: |
          docker buildx imagetools create -t ${{ env.DOCKER_USERNAME }}/pymes-frontend:staging \
            ${{ env.DOCKER_USERNAME }}/pymes-frontend:staging-amd64 \
            ${{ env.DOCKER_USERNAME }}/pymes-frontend:staging-arm64

  # ==================== DEPLOY TO STAGING ====================
  deploy-staging:
    name: Deploy to Staging Server
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_repository.full_name == github.repository && github.event.workflow_run.event == 'push' }}
    runs-on: ubuntu-latest
    needs: [build-and-test, docker-manifest]
    permissions:
      contents: read
    environment:
      name: staging
      url: http://staging.pymes-admin.com

    steps:
      - name: Checkout code
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          persist-credentials: false

      - name: Deploy via SSH
        uses: appleboy/ssh-action@0ff4204d59e8e51228ff73bce53f80d53301dee2 # v1.2.5
        with:
          host: ${{ secrets.STAGING_HOST }}
          username: ${{ secrets.STAGING_USER }}
          key: ${{ secrets.STAGING_SSH_KEY }}
          port: 22
          script: |
            set -e
            echo "Deploying version: ${{ needs.build-and-test.outputs.version }}"

            cd ~/pymes-admin
            git pull origin develop

            cat > .env <<EOF
            DOCKER_USERNAME=${{ secrets.DOCKER_USERNAME }}
            TAG=${{ needs.build-and-test.outputs.version }}
            DB_NAME=${{ secrets.DB_NAME }}
            DB_USERNAME=${{ secrets.DB_USERNAME }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            JWT_ACCESS_EXPIRATION=${{ secrets.JWT_ACCESS_EXPIRATION }}
            JWT_REFRESH_EXPIRATION=${{ secrets.JWT_REFRESH_EXPIRATION }}
            CORS_ALLOWED_ORIGINS=${{ secrets.CORS_ALLOWED_ORIGINS_STAGING }}
            GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
            GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}
            SPRING_MAIL_USERNAME=${{ secrets.SPRING_MAIL_USERNAME }}
            SPRING_MAIL_PASSWORD=${{ secrets.SPRING_MAIL_PASSWORD }}
            OAUTH2_REDIRECT_URI=${{ secrets.OAUTH2_REDIRECT_URI }}
            EOF

            docker compose pull
            docker compose up -d
            docker image prune -af

      - name: Verify deployment
        run: |
          echo "Deployment to staging triggered successfully."
